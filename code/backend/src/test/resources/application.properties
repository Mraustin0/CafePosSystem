# ponytail: H2 in PostgreSQL mode keeps tests Docker-free; switch to Testcontainers if migrations use Postgres-only SQL.
spring.datasource.url=jdbc:h2:mem:cafepos;MODE=PostgreSQL;DATABASE_TO_LOWER=TRUE
spring.datasource.username=sa
spring.datasource.password=
spring.jpa.hibernate.ddl-auto=validate
