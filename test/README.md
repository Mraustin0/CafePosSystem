# Test

Test source อยู่ที่ `code/backend/src/test/java` (ตามมาตรฐาน Maven)

รันทั้งหมด:

```bash
cd code/backend
./mvnw test
```

Test report: `code/backend/target/surefire-reports/` — ให้ copy report ฉบับส่งมาไว้ในโฟลเดอร์นี้
